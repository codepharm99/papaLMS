// Minimal runtime export for `@/lib/nextAuthOptions` to satisfy dynamic import during dev.
// If your project uses `next-auth`, replace this stub with your real `authOptions`.
export const authOptions = {};
